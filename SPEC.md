# SPEC.md — Superseded

This spec describes the original `rag_pipeline/` prototype from May 2026.
The current codebase has diverged significantly:
- Backend is now in `backend/` (FastAPI + SurrealDB)
- Frontend is in `new_frontend/` (Next.js)
- Docker Compose for deployment
- Docs at `docs/API.md` and `AGENTS.md` at repo root

See `AGENTS.md` for the current state, `docs/API.md` for API docs.
